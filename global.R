options(shiny.reactlog = TRUE) # for testing
  # when running, hit Ctrl-F3 to see the reactivity tree

library(shiny)
library(shinyjs)
library(DT)
library(dplyr)
library(tidyr)
library(reactlog)
library(shinyWidgets)